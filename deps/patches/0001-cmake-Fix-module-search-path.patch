From 318e33a7503eded3276a7f180ddf04e1e5a25f39 Mon Sep 17 00:00:00 2001
From: Alexander Shishkin <alex@ash.works>
Date: Sat, 22 Feb 2025 23:20:37 +0200
Subject: [PATCH] cmake: Fix module search path

Currently, the "cmake" directory at the top of source tree is made the
only element in the module search list, which breaks compilation as a
subdirectory of another project. Instead, add the current source
directory's "cmake" to the list of module search paths.

While at it, disable all compilation warnings.

Also, fix clock_gettime() tv_nsec calculation.

Also, instrument wmain() with _CrtDbgReport().

Also, hide wlibc_raise() calls behind WLIBC_SIGNALS.

Also, comment out SEH (__try/__except).

Also, replace the Rtl* unicode calls, which seem to be causing trouble.

Signed-off-by: Alexander Shishkin <alex@ash.works>
---
 CMakeLists.txt       |  4 +-
 src/internal/wmain.c | 98 ++++++++++++++++++++++++++++++++++++++++----
 src/sys/time/clock.c |  2 +-
 3 files changed, 94 insertions(+), 10 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index cb6030a..9dc9be8 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -18,7 +18,7 @@ add_compile_definitions(UNICODE _UNICODE
 
 if(CMAKE_C_COMPILER_ID STREQUAL "Clang")
 	add_compile_options(-fcolor-diagnostics -fansi-escape-codes
-						-Wall -Wextra
+						-w
 						-std=c11)
 endif()
 
@@ -30,7 +30,7 @@ if(CMAKE_C_COMPILER_ID STREQUAL "MSVC")
 						/std:c11)
 endif()
 
-set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake)
+list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
 include_directories(include)
 
 option(BUILD_SHARED_LIBS "Build Shared Libraries" OFF)
diff --git a/src/internal/wmain.c b/src/internal/wmain.c
index beb8d3c..b91a4c3 100644
--- a/src/internal/wmain.c
+++ b/src/internal/wmain.c
@@ -14,11 +14,14 @@
 #include <internal/thread.h>
 #include <internal/timer.h>
 #include <stdlib.h>
+#include <crtdbg.h>
 
 extern int main(int argc, char **argv, char **env);
 
+#ifdef WLIBC_ERRNO
 // From errno/program.c
 void init_program_name(void);
+#endif
 
 int exception_handler(DWORD code)
 {
@@ -30,7 +33,9 @@ int exception_handler(DWORD code)
 	case STATUS_ARRAY_BOUNDS_EXCEEDED:
 	case STATUS_GUARD_PAGE_VIOLATION:
 	case STATUS_IN_PAGE_ERROR:
+#ifdef WLIBC_SIGNALS
 		wlibc_raise(SIGSEGV);
+#endif
 		return EXCEPTION_CONTINUE_EXECUTION;
 
 	// SIGFPE
@@ -43,14 +48,18 @@ int exception_handler(DWORD code)
 	case STATUS_FLOAT_UNDERFLOW:
 	case STATUS_INTEGER_DIVIDE_BY_ZERO:
 	case STATUS_INTEGER_OVERFLOW:
+#ifdef WLIBC_SIGNALS
 		wlibc_raise(SIGFPE);
+#endif
 		return EXCEPTION_CONTINUE_EXECUTION;
 
 	// SIGILL
 	case STATUS_ILLEGAL_INSTRUCTION:
 	case STATUS_PRIVILEGED_INSTRUCTION:
 	case STATUS_STACK_OVERFLOW:
+#ifdef WLIBC_SIGNALS
 		wlibc_raise(SIGILL);
+#endif
 		return EXCEPTION_CONTINUE_EXECUTION;
 
 	default:
@@ -64,8 +73,14 @@ int wmain(int argc, wchar_t **wargv, wchar_t **wenv)
 	char **argv = NULL, **env = NULL;
 	UTF8_STRING *u8_args = NULL, *u8_env = NULL;
 
+#ifdef CLAP_DEBUG
+	_CrtSetReportMode(_CRT_WARN, _CRTDBG_MODE_FILE/* | _CRTDBG_MODE_WNDW*/);
+	_CrtSetReportFile(_CRT_WARN, _CRTDBG_FILE_STDERR);
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### entering wmain, argc=%d\n", argc);
+#endif
 	if (argc)
 	{
+#if 0
 		argv = (char **)RtlAllocateHeap(NtCurrentProcessHeap(), 0, sizeof(char *) * (argc + 1)); // argv ends with NULL
 		u8_args = (UTF8_STRING *)RtlAllocateHeap(NtCurrentProcessHeap(), 0, sizeof(UTF8_STRING) * argc);
 
@@ -91,8 +106,39 @@ int wmain(int argc, wchar_t **wargv, wchar_t **wenv)
 			argv[i] = u8_args[i].Buffer;
 		}
 		argv[argc] = NULL;
+#else
+	// Allocate an argv array with room for argc + 1 pointers.
+	argv = malloc((argc + 1) * sizeof(char *));
+	if (!argv) {
+		// Allocation failure; exit with error.
+		return EXIT_FAILURE;
 	}
 
+	// Convert each wide argument to a narrow string.
+	for (int i = 0; i < argc; i++) {
+		// Determine required length (including the null terminator).
+		size_t len = wcstombs(NULL, wargv[i], 0);
+		if (len == (size_t)-1) {
+			// Conversion error; use an empty string.
+			len = 0;
+		}
+		argv[i] = malloc(len + 1);
+		if (!argv[i]) {
+			// Free previously allocated strings if allocation fails.
+			for (int j = 0; j < i; j++) {
+				free(argv[j]);
+			}
+			free(argv);
+			return -1;
+		}
+		// Perform the conversion.
+		wcstombs(argv[i], wargv[i], len + 1);
+	}
+	argv[argc] = NULL;  // Null-terminate the argv array.
+#endif
+	}
+
+#if 0
 	if (wenv)
 	{
 		for (envc = 0; wenv[envc] != NULL; ++envc)
@@ -127,8 +173,12 @@ int wmain(int argc, wchar_t **wargv, wchar_t **wenv)
 		}
 		env[envc] = NULL;
 	}
+#endif
 
 #ifdef WLIBC_IO
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing IO\n");
+#endif
 	// DO NOT change the order of this.
 	init_fd_table();
 	initialize_stdio();
@@ -136,50 +186,83 @@ int wmain(int argc, wchar_t **wargv, wchar_t **wenv)
 	atexit(cleanup_stdio);
 #endif
 #ifdef WLIBC_SIGNALS
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing signals\n");
+#endif
 	signal_init();
 	atexit(signal_cleanup);
 #endif
 #ifdef WLIBC_SPAWN
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing spawn\n");
+#endif
 	process_init();
 	atexit(process_cleanup);
 #endif
 #ifdef WLIBC_THREADS
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing threads\n");
+#endif
 	threads_init();
 	atexit(threads_cleanup);
 #endif
 #ifdef WLIBC_ACLS
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing ACLs\n");
+#endif
 	initialize_sids();
 	atexit(cleanup_security_decsriptors);
 #endif
 #ifdef WLIBC_TIMERS
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing timers\n");
+#endif
 	initialize_itimers();
 	atexit(cleanup_itimers);
 #endif
 #ifdef WLIBC_ERRNO
+#ifdef CLAP_DEBUG
+	_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### initializing program name\n");
+#endif
 	init_program_name();
 #endif
 
 	int exit_status = 0;
 
-	__try
-	{
+	// __try
+	// {
+#ifdef CLAP_DEBUG
+		_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### calling main\n");
+#endif
 		exit_status = main(argc, argv, env);
-	}
-	__except (exception_handler(GetExceptionCode()))
-	{
-		;
-	}
+#ifdef CLAP_DEBUG
+		_CrtDbgReport(_CRT_WARN, __FILE__, __LINE__, "wlibc.lib", "### main returned=%d\n", exit_status);
+#endif
+	// }
+	// __except (exception_handler(GetExceptionCode()))
+	// {
+	// 	;
+	// }
 
 	if (argc)
 	{
+#if 0
 		for (int i = 0; i < argc; i++)
 		{
 			RtlFreeUTF8String(&u8_args[i]);
 		}
 		RtlFreeHeap(NtCurrentProcessHeap(), 0, u8_args);
 		RtlFreeHeap(NtCurrentProcessHeap(), 0, argv);
+#else
+		for (int i = 0; i < argc; i++)
+		{
+			free(argv[i]);
+		}
+		free(argv);
+#endif
 	}
 
+#if 0
 	if (wenv)
 	{
 		for (int i = 0; i < envc; i++)
@@ -189,6 +272,7 @@ int wmain(int argc, wchar_t **wargv, wchar_t **wenv)
 		RtlFreeHeap(NtCurrentProcessHeap(), 0, u8_env);
 		RtlFreeHeap(NtCurrentProcessHeap(), 0, env);
 	}
+#endif
 
 	return exit_status;
 }
diff --git a/src/sys/time/clock.c b/src/sys/time/clock.c
index 6698e04..3d11b5e 100644
--- a/src/sys/time/clock.c
+++ b/src/sys/time/clock.c
@@ -53,7 +53,7 @@ int wlibc_clock_gettime(clockid_t id, struct timespec *ts)
 
 	// The values reported here should be from January 1st 1601 UTC.
 	ts->tv_sec = epoch.QuadPart / 10000000;
-	ts->tv_nsec = (epoch.QuadPart % 10) * 100;
+	ts->tv_nsec = (epoch.QuadPart % 10000000) * 100;
 
 	return 0;
 }
-- 
2.47.1.windows.2

